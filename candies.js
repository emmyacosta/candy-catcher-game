

class FalingCandies{   // create class for the candy types  of the falling candy  //explosions extends this class to animate explosion
            constructor( x, y, color, points )
            this.x = x
            this.y = y
            this.color = color 

          //  color = Math.random()*360   use this later to animate explosions
}
        